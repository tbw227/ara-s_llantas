# Production Environment Configuration
# Copy this to .env and update with your actual values

NODE_ENV=production
PORT=8000

# Database Configuration (PostgreSQL)
# Option 1: Use connection string (recommended for Vercel Postgres, Railway, etc.)
DATABASE_URL=postgresql://user:password@host:5432/database_name

# Option 2: Use individual parameters
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=your_postgres_password_here
DB_NAME=aras_llantas
DB_PORT=5432
DB_SSL=false

# CORS Configuration
CORS_ORIGINS=https://your-domain.com,https://www.your-domain.com

# JWT Secret (generate a strong secret)
JWT_SECRET=your_very_strong_jwt_secret_here_at_least_32_characters

# Optional: Email configuration for contact forms
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_USER=your-email@your-domain.com
# SMTP_PASS=your-email-password
